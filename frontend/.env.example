# URL of the FastAPI backend. During local development this defaults to
# http://localhost:8000 if unset. In production, set this to your deployed
# backend's URL (e.g. https://nullarchive-api.onrender.com).
VITE_API_URL=http://localhost:8000
